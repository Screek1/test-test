{% extends 'base.html.twig' %}

{% block body %}
    {% include 'layouts/common/breadcrumbs.html.twig' with {
        array: [
            {
                crumb: 'Contact Us',
            }
        ]
    } %}

    {% include 'layouts/common/title-block.html.twig' with {
        text: 'Contact Us',
        tag: 'h1',
        wrapMod: 'container mb50',
    } only %}

    {% include 'layouts/common/title-block.html.twig' with {
        text: 'Dan Marusin PREC* - Renanza Realty Inc',
        tag: 'h3',
        wrapMod: 'container mb40',
    } only %}

    {% include 'layouts/sections/contacts-map.html.twig' with {
        address: contactsData.address,
        phone: contactsData.phone,
        email: contactsData.email,
        lat: contactsData.lat,
        lon: contactsData.lon,
    } only %}

    {% include 'layouts/sections/request.html.twig' %}

{#    {{ dump(contactsData) }}#}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('map') }}
{% endblock %}